% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Analysis_CalculateMain.R
\name{load_data_from_month_purpleAir}
\alias{load_data_from_month_purpleAir}
\title{Load monthly PurpleAir CSV exports}
\usage{
load_data_from_month_purpleAir(
  start_date_of_month,
  upload_root_folder = Sys.getenv("UPLOAD_ROOT_FOLDER")
)
}
\arguments{
\item{start_date_of_month}{Date or date-like value used to determine the target month.}

\item{upload_root_folder}{Character string specifying the root upload folder.
Defaults to \code{Sys.getenv("UPLOAD_ROOT_FOLDER")}.}
}
\value{
A named list containing \code{PurpleAirDay}, \code{PurpleAirHour}, and \code{PurpleAirInfo}.
}
\description{
Loads the PurpleAir monthly CSV exports for a target month. The function reads
the daily and hourly files, keys them by datasource ID extracted from the file
names, and returns the data structures used by the analysis reports.
}
\details{
\strong{Run:}
\enumerate{
\item Validates the upload root folder input.
\item Calculates the monthly folder name from the supplied date.
\item Reads daily and hourly PurpleAir CSV files.
\item Rewrites the value column names used by the report templates.
\item Loads monitor metadata for the PurpleAir sensors.
}

\strong{Data processing details:}
\itemize{
\item Datasource IDs are parsed from filenames using the embedded sensor identifier.
\item Files are kept only when a \code{time_stamp} column exists.
\item The returned lists are named by the parsed datasource ID.
\item The metadata table is filtered to the loaded device IDs before returning.
}

\strong{File structure:}
\preformatted{
upload_root_folder/
└── CSV/
    └── PurpleAir/
        └── PurpleAir.YYYY-MM-DD.YYYY-MM-DD/
            ├── *Daily*.csv
            └── *Hourly*.csv
}
}
\section{Error handling}{

The function stops with a clear message if \code{upload_root_folder} is missing or if the
monthly PurpleAir export folder cannot be read.
}

\examples{
\dontrun{
load_data_from_month_purpleAir(as.Date("2025-01-01"))
}

}
\seealso{
\code{\link{load_data_from_month_clarity}}
}
\concept{addRoxygenComments:true}
\concept{cleanupComments:true}
\concept{cleanupDependenciesNamespace:true}
\concept{cleanupParameters:true}
\concept{removedDependencies:true}
\concept{removedRawFunctionCalls:true}
\concept{removedSensitiveInfo:true}
\concept{role:process}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Download_PurpleAir.R
\name{save_purpleAir_to_csv}
\alias{save_purpleAir_to_csv}
\title{Download PurpleAir data and save to CSV files}
\usage{
save_purpleAir_to_csv(
  current_date,
  root_folder = Sys.getenv("UPLOAD_ROOT_FOLDER"),
  records_folder = Sys.getenv("RECORDS_ROOT_FOLDER"),
  purpleair_api_key = Sys.getenv("PURPLEAPI"),
  is_testing = FALSE
)
}
\arguments{
\item{current_date}{Date object used to determine the target month for data extraction.}

\item{root_folder}{Character string specifying the root folder path for file operations. Defaults to \code{Sys.getenv("UPLOAD_ROOT_FOLDER")}.}

\item{records_folder}{Character string specifying the folder path where the monitor tracking Excel file is located. Defaults to \code{Sys.getenv("RECORDS_ROOT_FOLDER")}.}

\item{purpleair_api_key}{Character string containing the PurpleAir API key. Defaults to \code{Sys.getenv("PURPLEAPI")}.}

\item{is_testing}{Logical flag indicating whether the function is being run in a testing environment. Defaults to \code{FALSE}.}
}
\value{
NULL (invisible). Called for side effects: creates CSV files and appends to a log.
}
\description{
Downloads PurpleAir sensor data for the previous month, saves daily and hourly
CSV exports for each sensor, and records a log entry to avoid duplicate runs.
}
\details{
\strong{Run:}
\enumerate{
\item \strong{Validation}: Checks required parameters and folder structure
\item \strong{Log check}: Verifies if data for the target month has already been processed
\item \strong{Time calculation}: Determines the previous month's date range for data extraction
\item \strong{Retrieve monitor info}: Retrieves sensor information from monitor tracking
\item \strong{API requests}: Downloads data for each sensor using the PurpleAir API
\item \strong{File operations}: Creates folder structure and saves CSV files (daily and hourly)
\item \strong{Logging}: Records completion status to prevent duplicate processing
}

\strong{Data processing details:}
\itemize{
\item Uses \code{previous_month_bounds()} to compute timestamps for the previous month
\item Splits and saves sensor data by device ID
}

\strong{File structure:}
\preformatted{
root_folder/
└── CSV/
    └── PurpleAir/
        └── PurpleAir.YYYY-MM-DD.YYYY-MM-DD/
            ├── [sensor files]
└── CSV/Exports/PurpleAirLog.csv
}
}
\section{Error handling}{

The function will stop with clear messages if required inputs are missing or if
folder creation or API calls fail. The function will not re-run for a month
already present in the log file.
}

\examples{
\dontrun{
save_purpleAir_to_csv(Sys.Date(), root_folder = "~/project_uploads")
}

}
\seealso{
\code{\link{previous_month_bounds}},
\code{\link{read_monitor_info_from_monitor_tracking}},
\code{\link{get_single_sensor_data_custom}},
\code{\link{save_aq_to_csv}}
}
\concept{addCheckSetupFolder:true}
\concept{addRoxygenComments:true}
\concept{cleanupComments:true}
\concept{cleanupDependenciesNamespace:true}
\concept{cleanupParameters:true}
\concept{removedDependencies:true}
\concept{removedRawFunctionCalls:true}
\concept{removedSensitiveInfo:true}
\concept{role:download}

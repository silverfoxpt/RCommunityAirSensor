% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Analysis_CalculateMain.R
\name{load_data_from_month_clarity}
\alias{load_data_from_month_clarity}
\title{Load monthly Clarity CSV exports}
\usage{
load_data_from_month_clarity(
  start_date_of_month,
  upload_root_folder = Sys.getenv("UPLOAD_ROOT_FOLDER")
)
}
\arguments{
\item{start_date_of_month}{Date or date-like value used to determine the target month.}

\item{upload_root_folder}{Character string specifying the root upload folder.
Defaults to \code{Sys.getenv("UPLOAD_ROOT_FOLDER")}.}
}
\value{
A named list containing \code{Day}, \code{Hour}, \code{DayReference}, \code{HourReference},
\code{Info}, \code{ReferenceInfo}, and \code{Date}.
}
\description{
Loads the Clarity and Clarity reference monthly CSV exports for a target month.
The function reads the daily and hourly files, keys them by datasource ID, and
returns the data structures used by the analysis reports.
}
\details{
\strong{Run:}
\enumerate{
\item Validates the upload root folder input.
\item Calculates the monthly folder name from the supplied date.
\item Reads daily and hourly Clarity CSV files.
\item Reads daily and hourly Clarity reference CSV files.
\item Loads monitor metadata for the Clarity sensors and reference sites.
}

\strong{Data processing details:}
\itemize{
\item Daily and hourly files are filtered from the monthly CSV export folders.
\item Files are kept only when they include a \code{datasourceId} column.
\item The returned lists are named by the first \code{datasourceId} value in each file.
\item Reference data are loaded from the \code{Clarity-Reference} export folder.
}

\strong{File structure:}
\preformatted{
upload_root_folder/
└── CSV/
    ├── Clarity/
    │   └── Clarity.YYYY-MM-DD.YYYY-MM-DD/
    │       ├── *Daily*.csv
    │       └── *Hourly*.csv
    └── Clarity-Reference/
        └── Clarity.YYYY-MM-DD.YYYY-MM-DD/
            ├── *Daily*.csv
            └── *Hourly*.csv
}
}
\section{Error handling}{

The function stops with a clear message if \code{upload_root_folder} is missing or if the
monthly Clarity export folders cannot be read.
}

\examples{
\dontrun{
load_data_from_month_clarity(as.Date("2025-01-01"))
}

}
\seealso{
\code{\link{load_data_from_month_purpleAir}}
}
\concept{addRoxygenComments:true}
\concept{cleanupComments:true}
\concept{cleanupDependenciesNamespace:true}
\concept{cleanupParameters:true}
\concept{removedDependencies:true}
\concept{removedRawFunctionCalls:true}
\concept{removedSensitiveInfo:true}
\concept{role:process}

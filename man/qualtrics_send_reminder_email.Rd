% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Qualtrics_Weekly_SendReminder.R
\name{qualtrics_send_reminder_email}
\alias{qualtrics_send_reminder_email}
\title{Send weekly Qualtrics reminder email to unresponsive personnel}
\usage{
qualtrics_send_reminder_email(
  current_date = Sys.Date(),
  upload_root_folder = Sys.getenv("UPLOAD_ROOT_FOLDER"),
  qualtrics_api_key = Sys.getenv("QUALTRICS_API_KEY"),
  directory_id = Sys.getenv("QUALTRICS_DIRECTORY_ID"),
  library_id = Sys.getenv("QUALTRICS_LIBRARY_ID"),
  message_id = Sys.getenv("QUALTRICS_REMINDER_MESSAGE_ID"),
  mailing_list_prefix = "Reminder: Weekly mailing list - ",
  email_subject_prefix = "Reminder: Weekly monitor health check survey - week of "
)
}
\arguments{
\item{current_date}{Date used to determine the start of the week. Defaults to \code{Sys.Date()}.}

\item{upload_root_folder}{Character string specifying the root upload folder. Defaults to \code{Sys.getenv("UPLOAD_ROOT_FOLDER")}.}

\item{qualtrics_api_key}{Character string containing the Qualtrics API key. Defaults to \code{Sys.getenv("QUALTRICS_API_KEY")}.}

\item{directory_id}{Character string containing the Qualtrics directory ID. Defaults to \code{Sys.getenv("QUALTRICS_DIRECTORY_ID")}.}

\item{library_id}{Character string containing the Qualtrics library ID. Defaults to \code{Sys.getenv("QUALTRICS_LIBRARY_ID")}.}

\item{message_id}{Character string containing the reminder message ID. Defaults to \code{Sys.getenv("QUALTRICS_REMINDER_MESSAGE_ID")}.}

\item{mailing_list_prefix}{Character string used to name the reminder mailing list. Defaults to \code{"Reminder: Weekly mailing list - "}.}

\item{email_subject_prefix}{Character string used as the reminder email subject prefix. Defaults to \code{"Reminder: Weekly monitor health check survey - week of "}.}
}
\value{
NULL. Called for side effects: sends a reminder and writes the weekly log entry.
}
\description{
Sends a reminder distribution for the current weekly survey to personnel who
have not yet responded. The workflow checks the weekly log, builds a mailing
list from the unresponsive participants, sends the reminder message, and
records completion in the weekly log.
}
\details{
\strong{Run:}
\enumerate{
\item \strong{Validation}: Checks the upload folder structure and required Qualtrics credentials.
\item \strong{Log check}: Reads the weekly log and skips execution if the survey response has not been retrieved or if the reminder was already sent.
\item \strong{Survey lookup}: Retrieves the survey id from the weekly \code{CreateSurvey} log entry.
\item \strong{Participant filtering}: Loads the processed response file and derives the unresponsive personnel list.
\item \strong{Mailing list creation}: Creates a Qualtrics mailing list for the unresponsive participants.
\item \strong{Distribution}: Sends the reminder distribution using the configured message and survey.
\item \strong{Logging}: Writes a \code{SendReminder} entry to the weekly log.
}

\strong{Data processing details:}
\itemize{
\item Uses the latest retrieved weekly response file recorded in the weekly log.
\item Derives the reminder recipient list from \code{get_unresponsed_personnel_list()}.
\item Keeps the reminder distribution aligned to the same weekly survey identifier used for creation.
}

\strong{File structure:}
\preformatted{
[upload_root_folder]/CSV/Exports/QualtricsWeeklyLog.csv
[upload_root_folder]/CSV/Imports/MainPersonnel.csv
[upload_root_folder]/CSV/Qualtrics/Weekly/
}
}
\section{Error handling}{

The function stops with informative messages if required credentials are missing, the upload folder structure is invalid, the weekly survey creation log entry cannot be found, or the response file name is missing from the weekly log.
}

\examples{
\dontrun{
qualtrics_send_reminder_email()
qualtrics_send_reminder_email(
  qualtrics_api_key = "<REDACTED_API_KEY>",
  directory_id = "<REDACTED_DIRECTORY_ID>",
  library_id = "<REDACTED_LIBRARY_ID>",
  message_id = "<REDACTED_MESSAGE_ID>"
)
}

}
\seealso{
\code{\link{get_weekly_log}}, \code{\link{get_first_save_data_from_weekly_log}},
\code{\link{get_unresponsed_personnel_list}}, \code{\link{create_and_add_contact_from_personnel_list}},
\code{\link{distribute_qualtrics_survey}}, \code{\link{write_to_weekly_log}}
}
\concept{addCheckSetupFolder:true}
\concept{addRoxygenComments:true}
\concept{cleanupComments:true}
\concept{cleanupDependenciesNamespace:true}
\concept{cleanupParameters:true}
\concept{removedDependencies:true}
\concept{removedRawFunctionCalls:true}
\concept{removedSensitiveInfo:true}
\concept{role:report}

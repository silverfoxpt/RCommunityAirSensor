% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Report_GeneratePurpleAir.R
\name{generate_purple_air_instant_report}
\alias{generate_purple_air_instant_report}
\title{Generate PurpleAir instant report}
\usage{
generate_purple_air_instant_report(
  current_date = lubridate::now(tzone = "UTC"),
  upload_root_folder = Sys.getenv("UPLOAD_ROOT_FOLDER"),
  records_root_folder = Sys.getenv("RECORDS_ROOT_FOLDER"),
  api_key = Sys.getenv("PURPLEAPI"),
  template_path = file.path("data-raw", "Report_PurpleAirTemplate.Rmd"),
  output_file = NULL,
  debug_turn_on = TRUE,
  online_threshold_seconds = 600,
  rate_delay_seconds = 2,
  request_pause_seconds = 2
)
}
\arguments{
\item{current_date}{Date or POSIXt value used to determine the previous month reporting window.}

\item{upload_root_folder}{Character string specifying the root folder for report output. Defaults to the \code{UPLOAD_ROOT_FOLDER} environment variable.}

\item{records_root_folder}{Character string specifying the root folder containing \code{CAMNMonitorTracking.xlsx}. Defaults to the \code{RECORDS_ROOT_FOLDER} environment variable.}

\item{api_key}{Character string containing the PurpleAir API key. Defaults to the \code{PURPLEAPI} environment variable.}

\item{template_path}{Character string specifying the R Markdown template used to render the report.}

\item{output_file}{Character string specifying the final PDF path. Defaults to a dated file under \code{upload_root_folder}.}

\item{debug_turn_on}{Logical indicating whether report debugging output should be enabled in the template. Defaults to \code{TRUE}.}

\item{online_threshold_seconds}{Numeric threshold in seconds for marking a device as online. Defaults to 600.}

\item{rate_delay_seconds}{Numeric delay in seconds used for the API rate limiter. Defaults to 2.}

\item{request_pause_seconds}{Numeric pause in seconds inserted after the status request. Defaults to 2.}
}
\value{
NULL. The function is called for its side effects.
}
\description{
Creates the monthly PurpleAir sensor health report, including current status,
recent hourly data checks, archive validation, and completeness checks, then
renders the result to a PDF report.
}
\details{
\strong{Run:}
\enumerate{
\item Validates the output root folder, records root folder, API key, and report template.
\item Determines the previous month reporting window and output location.
\item Reads PurpleAir monitor metadata from the tracking workbook.
\item Retrieves current status and recent hourly measurements from the PurpleAir API.
\item Evaluates sensor malfunction indicators, archive coverage, and completeness.
\item Renders the report template to a PDF file.
}

\strong{Data processing details:}
\itemize{
\item Uses the tracked sensor IDs to request status and hourly PurpleAir data.
\item Normalizes timestamps and quality checks before report rendering.
\item Splits recent hourly data by sensor ID for per-sensor QA/QC summaries.
\item Joins monitor short codes into each summary table for display in the report.
}

\strong{File structure:}
\preformatted{
[UPLOAD_ROOT_FOLDER]/CSV/Instant-Report/YYYY-MM/
  Status of Purple Air sensors - YYYY-MM-DD.pdf
}
}
\section{Error handling}{

Stops with a clear message if folders, credentials, the Excel tracking file, or the report template are missing or invalid.
}

\examples{
\dontrun{
generate_purple_air_instant_report()
}

}
\seealso{
\code{\link{read_monitor_info_from_monitor_tracking}},
\code{\link{test_sensor_malfunction_result_report_purpleAir}},
\code{\link{test_full_headers_report_purpleAir}},
\code{\link{check_missing_data_report_purpleAir}}
}
\concept{addCheckSetupFolder:true}
\concept{addRoxygenComments:true}
\concept{cleanupComments:true}
\concept{cleanupDependenciesNamespace:true}
\concept{cleanupParameters:true}
\concept{removedDependencies:true}
\concept{removedRawFunctionCalls:true}
\concept{removedSensitiveInfo:true}
\concept{role:report}
